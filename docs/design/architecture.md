# 🏗️ System Architecture — CloudSetup.guide

> 최종 수정: 2025.02.07
> 버전: v1.0

---

## 1. 아키텍처 개요

CloudSetup.guide는 **정적 사이트 우선(SSG-First)** 아키텍처를 채택합니다. 콘텐츠는 빌드 시 정적 HTML로 생성되어 CDN에서 제공되며, 동적 기능은 Phase 3부터 점진적으로 추가됩니다.

```
┌──────────────────────────────────────────────────────────────┐
│                     전체 시스템 아키텍처                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  사용자 브라우저                                               │
│  ┌───────────────────────────────────────────────────────┐   │
│  │ Next.js App (React) + Tailwind CSS + shadcn/ui        │   │
│  │ ├── 정적 HTML (SSG) ← CDN에서 즉시 제공               │   │
│  │ ├── 클라이언트 JS (하이드레이션)                        │   │
│  │ │   ├── Pagefind (검색)                               │   │
│  │ │   ├── GA4 (분석)                                    │   │
│  │ │   ├── AdSense (광고)                                │   │
│  │ │   └── Supabase SDK (Phase 3, 인증/DB)               │   │
│  │ └── localStorage (비로그인 진행률)                      │   │
│  └───────────┬──────────────────────────────┬────────────┘   │
│              │                              │                │
│              ▼                              ▼                │
│  ┌──── Vercel ──────────┐    ┌──── Supabase ──────────┐     │
│  │                      │    │                        │     │
│  │  Edge Network (CDN)  │    │  Auth (Google, GitHub) │     │
│  │  ├── 정적 HTML       │    │  PostgreSQL DB         │     │
│  │  ├── 이미지 (최적화)  │    │  ├── profiles         │     │
│  │  └── 검색 인덱스      │    │  ├── guide_progress   │     │
│  │                      │    │  ├── comments          │     │
│  │  Serverless Functions │    │  ├── feedback          │     │
│  │  ├── /api/feedback   │    │  └── newsletter_subs   │     │
│  │  └── /api/newsletter │    │                        │     │
│  │                      │    │  RLS (Row Level Sec.)  │     │
│  └──────────────────────┘    └────────────────────────┘     │
│              │                                               │
│              ▼                                               │
│  ┌──── External ────────────────────────────────────────┐   │
│  │                                                      │   │
│  │  Cloudinary ──── 이미지 CDN + 자동 최적화             │   │
│  │  Google Analytics 4 ──── 사용자 행동 분석             │   │
│  │  Google AdSense ──── 광고 수익                       │   │
│  │  Google Search Console ──── SEO 모니터링              │   │
│  │                                                      │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 2. Phase별 아키텍처 진화

### Phase 1: 순수 정적 사이트

```
GitHub (소스) → Vercel (빌드+CDN) → 사용자 브라우저
                    │
                    ├── 정적 HTML (SSG)
                    ├── 이미지 (next/image 최적화)
                    ├── Pagefind 검색 인덱스
                    └── 클라이언트: GA4, AdSense

특징:
- 서버 없음 (Serverless도 없음)
- DB 없음
- 인증 없음
- 모든 것이 빌드 시 결정됨
- CDN에서 제공 → 극한의 속도
```

### Phase 2: 정적 + 최소 API

```
GitHub → Vercel (빌드+CDN+Serverless) → 사용자 브라우저
              │                              │
              ├── 정적 HTML                   ├── GA4
              ├── Serverless Functions        ├── AdSense
              │   ├── /api/feedback → ─────── ├── Pagefind
              │   └── /api/newsletter → ───── │
              │                          │    │
              └──────────────────────────┘    │
                         │                    │
                         ▼                    │
                    Supabase DB               │
                    (feedback, newsletter만)   │

특징:
- 최소한의 Serverless 함수 (피드백, 뉴스레터)
- Supabase DB 사용하나 서비스키로만 (RLS 불필요)
- 사용자 인증 없음
```

### Phase 3: 정적 + 동적 하이브리드

```
GitHub → Vercel (빌드+CDN+Serverless) → 사용자 브라우저
              │                              │
              ├── 정적 HTML (가이드 본문)     ├── Supabase SDK (직접 접근)
              ├── Serverless Functions        │   ├── Auth
              │   ├── /api/feedback          │   ├── guide_progress
              │   └── /api/newsletter        │   └── comments
              │                              │
              └──────────────────────────────┘
                         │
                         ▼
                    Supabase
                    ├── Auth (Google, GitHub OAuth)
                    ├── DB (PostgreSQL + RLS)
                    └── Realtime (댓글 알림, 선택적)

특징:
- 정적 콘텐츠 + 동적 사용자 데이터 하이브리드
- Supabase RLS로 보안
- 클라이언트에서 Supabase 직접 접근 (API Route 최소화)
```

---

## 3. 컴포넌트 아키텍처

### 3.1 서버/클라이언트 컴포넌트 분리

Next.js App Router에서는 서버 컴포넌트(RSC)와 클라이언트 컴포넌트를 명확히 분리합니다.

```
서버 컴포넌트 (기본, 'use server' 불필요):
├── app/[locale]/page.tsx              — 홈페이지
├── app/[locale]/guides/[slug]/page.tsx — 가이드 상세
├── app/[locale]/platforms/*/page.tsx   — 플랫폼 페이지
├── components/guide/GuideHeader.tsx    — 가이드 메타 정보
├── components/guide/GuideFooter.tsx    — 완료 + 추천
├── components/platform/PlatformCard.tsx — 플랫폼 카드
└── components/common/Footer.tsx        — 푸터

클라이언트 컴포넌트 ('use client' 필요):
├── components/guide/ProgressBar.tsx     — 인터랙티브 진행률
├── components/guide/Checkpoint.tsx      — 체크/실패 토글
├── components/guide/CopyBlock.tsx       — 클립보드 복사
├── components/guide/DevTip.tsx          — 접힘/펼침
├── components/guide/Screenshot.tsx      — 라이트박스 확대
├── components/guide/GuideFeedback.tsx   — 피드백 위젯
├── components/guide/TableOfContents.tsx — 스크롤 추적 목차
├── components/explore/SearchBar.tsx     — Pagefind 검색
├── components/explore/FilterBar.tsx     — 필터 상태 관리
├── components/tools/FreeTierCalculator.tsx — 계산기 폼
├── components/common/Header.tsx         — 모바일 메뉴 토글
├── components/common/LanguageSwitcher.tsx — 언어 전환
├── components/common/ThemeToggle.tsx     — 다크모드
├── components/common/AdBanner.tsx        — 광고 lazy loading
└── components/auth/LoginModal.tsx        — Phase 3 로그인
```

### 3.2 컴포넌트 의존성 트리 (가이드 페이지)

```
app/[locale]/guides/[slug]/page.tsx (서버)
│
├── GuideHeader (서버)
│   ├── PlatformBadge (서버)
│   ├── DifficultyBadge (서버)
│   └── FreeTierBadge (서버)
│
├── PrerequisiteCheck (클라이언트)
│   └── localStorage 접근
│
├── ProgressBar (클라이언트)
│   └── IntersectionObserver
│
├── TableOfContents (클라이언트)
│   └── IntersectionObserver + scrollTo
│
├── MDXRemote (서버, 각 컴포넌트 매핑)
│   ├── Step (서버)
│   │   ├── Screenshot (클라이언트) → 라이트박스
│   │   ├── Callout (서버)
│   │   ├── CopyBlock (클라이언트) → clipboard
│   │   ├── FreeTierInfo (서버)
│   │   ├── DevTip (클라이언트) → 토글
│   │   └── Checkpoint (클라이언트) → 상태 관리
│   └── ... (Step 반복)
│
├── GuideNavigation (서버)
│
├── GuideFeedback (클라이언트)
│   └── fetch /api/feedback
│
├── AdBanner (클라이언트)
│   └── IntersectionObserver + AdSense
│
├── GuideFooter (서버)
│   ├── NextGuideCards (서버)
│   └── AffiliateLinks (서버)
│
└── CommentsSection (클라이언트, Phase 3)
    └── Supabase SDK
```

---

## 4. 데이터 흐름 아키텍처

### 4.1 빌드 타임 데이터 흐름

```
┌─ 빌드 시 (next build) ─────────────────────────────────┐
│                                                         │
│  content/guides/*/meta.json                             │
│        │                                                │
│        ▼                                                │
│  lib/guides.ts::getAllGuides()                          │
│        │                                                │
│        ├──→ generateStaticParams()                      │
│        │    → /ko/guides/gcp-vision-api-setup           │
│        │    → /en/guides/gcp-vision-api-setup           │
│        │    → ... (모든 가이드 × 모든 언어)              │
│        │                                                │
│        ├──→ 홈페이지: 인기 가이드, 플랫폼별 카운트       │
│        │                                                │
│        ├──→ sitemap.xml                                 │
│        │                                                │
│        └──→ Pagefind: 검색 인덱스 생성                   │
│                                                         │
│  content/guides/*/ko.mdx, en.mdx                       │
│        │                                                │
│        ▼                                                │
│  MDX 컴파일 → React 컴포넌트 매핑 → 정적 HTML           │
│                                                         │
│  data/free-tiers.json                                   │
│        │                                                │
│        ▼                                                │
│  무료 한도 대시보드 페이지 → 정적 HTML                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
         │
         ▼ (빌드 결과물)
┌─ .next/ ────────────────────────────────────────────────┐
│  server/app/ko/guides/gcp-vision-api-setup.html        │
│  server/app/en/guides/gcp-vision-api-setup.html        │
│  ... (수백 개 정적 HTML)                                │
│  static/ (JS 번들, CSS)                                 │
│  pagefind/ (검색 인덱스)                                 │
└─────────────────────────────────────────────────────────┘
         │
         ▼ (Vercel에 업로드)
┌─ Vercel Edge Network (글로벌 CDN) ─────────────────────┐
│  사용자 요청 → 가장 가까운 Edge에서 정적 HTML 즉시 응답   │
└─────────────────────────────────────────────────────────┘
```

### 4.2 런타임 데이터 흐름

```
┌─ 사용자 브라우저 ─────────────────────────────────────┐
│                                                       │
│  1. 정적 HTML 수신 (CDN) ← 대부분의 콘텐츠             │
│                                                       │
│  2. JS 하이드레이션 후 클라이언트 인터랙션:              │
│     │                                                 │
│     ├── [검색] → Pagefind (클라이언트, 네트워크 불필요)  │
│     │                                                 │
│     ├── [코드 복사] → clipboard API (로컬)              │
│     │                                                 │
│     ├── [진행률 체크] → localStorage (비로그인)          │
│     │                → Supabase (로그인, Phase 3)      │
│     │                                                 │
│     ├── [피드백 제출] → POST /api/feedback              │
│     │                  → Vercel Serverless             │
│     │                  → Supabase DB                   │
│     │                                                 │
│     ├── [댓글 작성] → Supabase SDK (직접, Phase 3)     │
│     │                                                 │
│     ├── [GA4 이벤트] → Google Analytics (비동기)        │
│     │                                                 │
│     └── [광고 로드] → AdSense (지연 로딩)               │
│                                                       │
└───────────────────────────────────────────────────────┘
```

---

## 5. 배포 아키텍처

### 5.1 CI/CD 파이프라인

```
개발자 워크플로우:

1. 로컬 개발:
   ┌──────────────┐
   │ 코드/MDX 수정 │
   │ npm run dev  │
   │ 로컬 프리뷰   │
   └──────┬───────┘
          │ git push
          ▼

2. GitHub:
   ┌──────────────────────┐
   │ PR 생성 or main push │
   │                      │
   │ ① PR인 경우:          │
   │   → Vercel Preview   │
   │   → 프리뷰 URL 생성   │
   │   → 리뷰 후 머지      │
   │                      │
   │ ② main push인 경우:   │
   │   → Vercel Production │
   └──────┬───────────────┘
          │ webhook
          ▼

3. Vercel 빌드:
   ┌──────────────────────────────────────┐
   │ npm install                          │
   │ npm run build                        │
   │   ├── next build (SSG)               │
   │   ├── pagefind 인덱스 생성            │
   │   └── validate-guides.ts 실행        │
   │       (meta.json 유효성 검사)         │
   │                                      │
   │ 빌드 성공 → Edge Network에 배포       │
   │ 빌드 실패 → 슬랙/이메일 알림         │
   └──────────────────────────────────────┘

4. 배포 후:
   ┌──────────────────────────────────────┐
   │ CDN 캐시 무효화 (변경된 페이지만)      │
   │ 검색 인덱스 업데이트                   │
   │ sitemap.xml 갱신                      │
   └──────────────────────────────────────┘
```

### 5.2 환경 구성

```
┌─ Development ──────────────────┐
│ URL: http://localhost:3000     │
│ DB: Supabase 개발 프로젝트      │
│ 광고: 비활성화                  │
│ 분석: 비활성화                  │
└────────────────────────────────┘

┌─ Preview (PR별) ───────────────┐
│ URL: https://xxx.vercel.app    │
│ DB: Supabase 개발 프로젝트      │
│ 광고: 비활성화                  │
│ 분석: 비활성화                  │
└────────────────────────────────┘

┌─ Production ───────────────────┐
│ URL: https://cloudsetup.guide  │
│ DB: Supabase 프로덕션 프로젝트  │
│ 광고: AdSense 활성화           │
│ 분석: GA4 활성화               │
└────────────────────────────────┘
```

---

## 6. 성능 아키텍처

### 6.1 로딩 성능 최적화

```
사용자가 가이드 페이지 요청
    │
    ▼
① CDN에서 정적 HTML 즉시 응답 (TTFB < 100ms)
    │
    ▼
② HTML 파싱 + 크리티컬 CSS 인라인
    │
    ├── <head>: 메타 태그, 폰트 preload, 크리티컬 CSS
    │
    ├── 첫 스크린샷: priority loading (LCP 대상)
    │
    └── 나머지 스크린샷: lazy loading (loading="lazy")
    │
    ▼
③ JS 하이드레이션 (최소 번들)
    │
    ├── 핵심 인터랙션 (ProgressBar, CopyBlock): 즉시 로드
    │
    ├── 검색 (Pagefind): 검색바 포커스 시 동적 import
    │
    ├── 광고 (AdSense): IntersectionObserver로 뷰포트 진입 시 로드
    │
    └── 댓글 (Phase 3): 댓글 섹션 스크롤 도달 시 동적 import
```

### 6.2 번들 크기 전략

```
목표: 초기 JS 번들 < 100KB (gzipped)

┌─ 코드 스플리팅 전략 ────────────────────────┐
│                                             │
│ 핵심 번들 (모든 페이지):        ~40KB        │
│ ├── React runtime                           │
│ ├── next/navigation                         │
│ └── 글로벌 레이아웃                          │
│                                             │
│ 가이드 페이지 번들:             ~30KB        │
│ ├── ProgressBar                             │
│ ├── CopyBlock                               │
│ ├── Checkpoint                              │
│ ├── DevTip                                  │
│ └── Screenshot (라이트박스)                   │
│                                             │
│ 지연 로드 (필요 시):                         │
│ ├── Pagefind          ~15KB (검색 시)       │
│ ├── AdSense           ~20KB (뷰포트 진입 시) │
│ ├── Calculator        ~10KB (계산기 페이지만) │
│ ├── Comments          ~15KB (Phase 3)       │
│ └── Auth              ~10KB (Phase 3)       │
│                                             │
└─────────────────────────────────────────────┘
```

### 6.3 이미지 최적화 파이프라인

```
원본 스크린샷 (PNG, ~500KB)
    │
    ▼
Cloudinary 업로드
    │
    ├── 자동 형식 변환 (f_auto → WebP/AVIF)
    ├── 자동 품질 조절 (q_auto)
    ├── 반응형 리사이징:
    │   ├── w_400 (모바일)
    │   ├── w_640 (태블릿)
    │   └── w_800 (데스크톱)
    └── blur placeholder 생성 (w_20, e_blur:1000)
    │
    ▼
next/image 컴포넌트:
    ├── srcSet: 400w, 640w, 800w
    ├── sizes: "(max-width: 768px) 100vw, 800px"
    ├── placeholder: "blur"
    ├── priority: 첫 스크린샷만 true
    └── loading: "lazy" (나머지)
    │
    ▼
최종 사이즈: ~50-80KB per 이미지 (원본 대비 85-90% 감소)
```

---

## 7. 보안 아키텍처

### 7.1 보안 계층

```
┌─ Layer 1: 네트워크 ──────────────────────────────────┐
│ ✅ HTTPS 강제 (Vercel 자동 SSL/TLS)                   │
│ ✅ HSTS 헤더                                          │
│ ✅ Vercel DDoS 보호                                   │
└──────────────────────────────────────────────────────┘

┌─ Layer 2: 애플리케이션 ──────────────────────────────┐
│ ✅ CSP (Content Security Policy) 헤더                 │
│    script-src 'self' *.googletagmanager.com           │
│               *.google-analytics.com                  │
│               *.googlesyndication.com                 │
│               *.pagefind.app                          │
│    img-src 'self' res.cloudinary.com *.supabase.co    │
│                                                      │
│ ✅ XSS 방지: MDX 렌더링 시 HTML sanitization          │
│ ✅ CSRF: SameSite 쿠키 + Origin 검증                  │
│ ✅ Rate Limiting: API Routes에 IP 기반 제한           │
└──────────────────────────────────────────────────────┘

┌─ Layer 3: 데이터 ────────────────────────────────────┐
│ ✅ Supabase RLS: 테이블별 행 수준 접근 제어            │
│ ✅ 서비스키 분리: SUPABASE_SERVICE_ROLE_KEY는           │
│    서버 사이드에서만 사용, 클라이언트 노출 금지          │
│ ✅ 환경변수: Vercel Environment Variables에 저장       │
│ ✅ 개인정보: 이메일만 수집, 결제 정보 없음              │
└──────────────────────────────────────────────────────┘

┌─ Layer 4: 의존성 ────────────────────────────────────┐
│ ✅ Dependabot: npm 패키지 자동 업데이트                │
│ ✅ npm audit: 빌드 시 취약점 검사                      │
│ ✅ 최소 의존성: 필요한 패키지만 설치                    │
└──────────────────────────────────────────────────────┘
```

### 7.2 인증 흐름 (Phase 3)

```
┌─ OAuth 2.0 (Google/GitHub) ──────────────────────────┐
│                                                      │
│ 1. 사용자 → "Google로 로그인" 클릭                     │
│ 2. → Supabase Auth → Google OAuth 페이지 리다이렉트   │
│ 3. 사용자 → Google에서 인증 완료                      │
│ 4. → Google → Supabase 콜백 URL                      │
│ 5. → Supabase → JWT 토큰 발급                       │
│ 6. → 브라우저 쿠키에 세션 저장                        │
│ 7. → 이후 요청에 JWT 자동 포함                       │
│ 8. → Supabase RLS가 JWT의 user_id로 접근 제어        │
│                                                      │
│ 토큰 관리:                                           │
│ ├── Access Token: 1시간 (자동 갱신)                   │
│ ├── Refresh Token: 30일                              │
│ └── 쿠키: httpOnly, secure, SameSite=Lax             │
│                                                      │
└──────────────────────────────────────────────────────┘
```

---

## 8. 모니터링 아키텍처

```
┌─ 성능 모니터링 ──────────────────────────────────────┐
│                                                      │
│ Vercel Analytics (무료):                              │
│ ├── Core Web Vitals (LCP, FID, CLS)                  │
│ ├── 페이지별 로딩 성능                                │
│ └── 지역별 응답 시간                                  │
│                                                      │
│ Google Search Console:                               │
│ ├── 색인 상태                                        │
│ ├── 검색 키워드 순위                                  │
│ ├── 클릭률 (CTR)                                     │
│ └── Core Web Vitals (실제 사용자)                     │
│                                                      │
└──────────────────────────────────────────────────────┘

┌─ 비즈니스 모니터링 ──────────────────────────────────┐
│                                                      │
│ Google Analytics 4:                                  │
│ ├── 실시간 사용자                                     │
│ ├── 가이드 완료율 (커스텀 이벤트)                      │
│ ├── 단계별 이탈률                                     │
│ ├── 검색 쿼리 (사이트 내)                             │
│ ├── 트래픽 소스 (유기, 직접, 리퍼럴)                   │
│ └── 제휴 링크 클릭률                                  │
│                                                      │
│ AdSense 대시보드:                                    │
│ ├── RPM / CPC / CTR                                 │
│ ├── 페이지별 수익                                     │
│ └── 광고 최적화 제안                                  │
│                                                      │
└──────────────────────────────────────────────────────┘

┌─ 가용성 모니터링 ────────────────────────────────────┐
│                                                      │
│ UptimeRobot (무료):                                  │
│ ├── 5분 간격 HTTP 체크                               │
│ ├── 다운타임 알림 (이메일)                             │
│ └── 월간 업타임 리포트                                │
│                                                      │
│ Vercel 빌드 알림:                                    │
│ ├── 빌드 실패 시 이메일/슬랙 알림                     │
│ └── 빌드 시간 모니터링                                │
│                                                      │
└──────────────────────────────────────────────────────┘
```

---

## 9. 확장성 설계

### 9.1 콘텐츠 확장

```
현재: 10개 가이드 → 목표: 100+ 가이드

확장 방법: MDX 파일 추가만으로 자동 확장
├── 새 디렉토리: content/guides/new-guide-slug/
├── meta.json + ko.mdx + en.mdx + screenshots/
├── git push → Vercel 자동 빌드
└── 사이트맵, 검색 인덱스 자동 갱신

빌드 시간 예상:
├── 10 가이드: ~30초
├── 50 가이드: ~1분
├── 100 가이드: ~2분
└── 500 가이드: ~5분 (ISR 전환 검토 시점)
```

### 9.2 트래픽 확장

```
SSG + CDN이므로 서버 스케일링 불필요

├── 월 10만 PV: Vercel 무료 티어 충분 (100GB 대역폭)
├── 월 50만 PV: Vercel Pro ($20/월, 1TB 대역폭)
├── 월 200만 PV: Vercel Pro 충분
└── 월 1000만 PV: Vercel Enterprise 또는 CDN 추가 검토

병목 가능 지점:
├── Supabase DB: 무료 티어 50,000 MAU → Pro $25/월
├── 이미지 CDN: Cloudinary 무료 25GB → 유료 전환
└── Pagefind: 클라이언트 사이드이므로 서버 부하 없음
```

### 9.3 기능 확장

```
Phase 4+ 확장 시 아키텍처 변경 가능성:

① ISR (Incremental Static Regeneration):
   가이드 수가 500개 이상이면 빌드 시간 최적화를 위해
   revalidate: 3600 (1시간) 설정

② Edge Functions:
   지역별 맞춤 콘텐츠 (리전 추천 등)

③ Supabase Realtime:
   댓글 실시간 알림, 사용자 간 상호작용

④ 별도 Admin 대시보드:
   콘텐츠 관리, 피드백 확인, 분석 대시보드
   → 별도 Next.js 앱 또는 Supabase Studio 활용
```

---

## 10. 재해 복구 (Disaster Recovery)

```
데이터 백업:
├── 콘텐츠 (MDX, JSON): GitHub에 버전 관리 → 유실 불가
├── Supabase DB: 자동 일일 백업 (Pro 플랜)
├── 이미지: Cloudinary 자체 백업 + 원본 로컬 보관
└── 환경변수: Vercel + 로컬 .env.local 이중 보관

복구 시나리오:
├── Vercel 장애: CDN이므로 자동 복구 대기 (SLA 99.99%)
├── Supabase 장애: 정적 콘텐츠는 정상, 동적 기능만 일시 중단
├── GitHub 장애: 로컬 git clone이 있으므로 복구 가능
└── 도메인 장애: DNS 변경으로 다른 CDN으로 전환 가능
```

---

## 11. 아키텍처 결정 기록 (ADR)

### ADR-001: SSG 우선 아키텍처

**결정:** SSG(Static Site Generation)를 기본으로, 동적 기능은 클라이언트 사이드에서 처리
**이유:** SEO가 생명인 사이트. SSR보다 SSG가 응답속도, CDN 캐싱, 비용 모든 면에서 우월
**트레이드오프:** 콘텐츠 업데이트 시 재빌드 필요 (실시간 반영 불가)

### ADR-002: Supabase 직접 접근 (API Route 미경유)

**결정:** Phase 3의 사용자 데이터는 Supabase Client SDK로 직접 접근, Next.js API Route를 최소화
**이유:** RLS가 보안을 담당하므로 중간 API 레이어 불필요. 개발 속도 및 유지보수 비용 절감
**트레이드오프:** 비즈니스 로직이 클라이언트에 분산될 수 있음

### ADR-003: MDX 기반 콘텐츠 관리

**결정:** CMS(Contentful, Sanity 등) 대신 Git 기반 MDX 파일로 콘텐츠 관리
**이유:** CMS 비용 0원, 버전 관리 자연스러움, 오픈소스 기여에 적합, 마이그레이션 용이
**트레이드오프:** 비개발자 기여자의 콘텐츠 편집이 어려움 (Markdown 이해 필요)

### ADR-004: Pagefind (정적 검색)

**결정:** Algolia 대신 Pagefind 사용 (Phase 1-2)
**이유:** 무료, 서버 불필요 (클라이언트 실행), 빌드 시 자동 인덱싱
**트레이드오프:** 실시간 인덱싱 불가, 고급 검색 기능 제한. Phase 3에서 Algolia로 전환 가능

### ADR-005: Cloudinary (이미지 CDN)

**결정:** Vercel Image Optimization 대신 Cloudinary 사용
**이유:** 무료 25GB, 자동 형식 변환(WebP/AVIF), URL 기반 리사이징, 스크린샷 관리에 최적
**트레이드오프:** 외부 서비스 의존성 추가

---

*이것으로 기술 설계 문서가 모두 완료되었습니다.*

**전체 문서 목록:**

기획:
1. idea.md — 아이디어
2. market.md — 시장 분석
3. persona.md — 사용자 페르소나
4. user_journey.md — 사용자 여정
5. business_model.md — 비즈니스 모델
6. product.md — 제품 설계
7. features.md — 기능 명세
8. tech.md — 기술 스택
9. roadmap.md — 개발 로드맵

기술 설계:
10. screen.md — 화면 설계
11. data_model.md — 데이터 모델
12. task_flow.md — 태스크 플로우
13. api.md — API 설계
14. architecture.md — 시스템 아키텍처
