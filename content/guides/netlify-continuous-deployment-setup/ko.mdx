# Netlify 연속 배포 설정 - GitHub 자동 배포

Netlify는 배포를 **자동이고 원활하게** 만듭니다. GitHub에 코드를 푸시할 때마다 웹사이트가 자동으로 업데이트됩니다. 20분 안에 연속 배포를 설정합니다.

<FreeTierInfo
  service="Netlify"
  limit="월 100 GB 대역폭"
  period="월간"
  status="generous"
/>

## 시작하기 전에

<PrerequisiteCheck items={[
  { text: "Netlify 계정 설정 완료", required: true },
  { text: "저장소가 있는 GitHub 계정", required: true },
  { text: "HTML/React/Next.js 코드가 있는 저장소", required: true }
]} />

<Callout type="info">
🔄 **연속 배포란?** GitHub에 코드를 푸시할 때마다 Netlify가 자동으로 웹사이트를 빌드하고 배포합니다. 수동 단계가 필요 없습니다 - 푸시하면 사이트가 즉시 업데이트됩니다.
</Callout>

---

<Step number={1} title="GitHub 저장소를 Netlify에 연결">

1. **Netlify 대시보드**에 로그인합니다.
2. **새 사이트 추가** 버튼을 클릭합니다.
3. **기존 프로젝트 가져오기**를 선택합니다.
4. Git 제공자로 **GitHub**을 선택합니다.
5. 목록에서 **저장소**를 선택합니다.
6. **사이트 배포** 버튼을 클릭합니다.

<Screenshot
  src="https://res.cloudinary.com/demo/image/upload/v1/netlify/deploy-step1-import-ko.png"
  alt="GitHub 저장소를 Netlify로 가져오기"
  caption="GitHub에서 저장소 선택"
  priority={true}
/>

<Callout type="tip">
💡 **팁**: 저장소가 나타나지 않으면 Netlify 계정 설정에서 GitHub 액세스가 인증되었는지 확인합니다.
</Callout>

</Step>

---

<Step number={2} title="빌드 설정 구성">

Netlify는 프로젝트를 기반으로 빌드 설정을 자동 감지합니다. 검토합니다:

1. **기본 디렉토리**: 보통 비어 있음 (저장소 루트).
2. **빌드 명령**:
   - React/Next.js: `npm run build`
   - 정적 HTML: 비워 둡니다
   - Hugo: `hugo`
3. **게시 디렉토리**:
   - React/Next.js: `out` 또는 `build`
   - 정적 HTML: `public` 또는 `.`
4. **사이트 배포** 버튼을 클릭합니다.

<Screenshot
  src="https://res.cloudinary.com/demo/image/upload/v1/netlify/deploy-step2-settings-ko.png"
  alt="빌드 설정 구성"
  caption="빌드 명령 및 출력 디렉토리 구성"
/>

<CopyBlock
  code="// 다양한 프레임워크에 대한 일반적인 빌드 설정:
// React: npm run build - Output: build
// Next.js: npm run build - Output: .next
// Hugo: hugo - Output: public
// Vue: npm run build - Output: dist
// 정적 HTML: (빌드 없음) - Output: . or public"
  language="text"
/>

</Step>

---

<Step number={3} title="초기 배포 완료 대기">

Netlify가 자동으로 사이트를 빌드하기 시작합니다. 다음을 볼 것입니다:

1. 배포 진행 상황을 보여주는 **빌드 로그**.
2. **상태**가 "빌드 중"에서 "게시됨"으로 변경됩니다.
3. **라이브 URL**이 나타납니다 (예: `happy-kangaroo-123.netlify.app`).
4. **라이브 URL**을 클릭하여 배포된 사이트를 봅니다.

<Screenshot
  src="https://res.cloudinary.com/demo/image/upload/v1/netlify/deploy-step3-building-ko.png"
  alt="배포 빌드 상태"
  caption="실시간으로 사이트가 빌드하고 배포되는 것을 봅니다"
/>

<Callout type="info">
⏳ **빌드 시간**: 첫 배포는 프로젝트에 따라 1-5분이 걸립니다. 후속 배포는 캐싱으로 보통 더 빠릅니다.
</Callout>

</Step>

---

<Step number={4} title="자동 배포 테스트">

이제 배포가 자동으로 작동하는지 테스트합니다:

1. **GitHub 저장소**로 돌아갑니다.
2. **작은 변경**을 코드에 합니다 (예: HTML의 제목 변경).
3. **커밋하고 푸시**합니다:

<CopyBlock
  code="git add .
git commit -m '자동 배포 테스트'
git push origin main"
  language="bash"
/>

4. Netlify 대시보드로 돌아갑니다.
5. **새 배포가 자동으로 시작**된 것을 봐야 합니다!
6. 완료될 때까지 기다립니다 (1-2분).
7. 라이브 사이트를 새로고침합니다 - 변경사항을 볼 것입니다!

<Screenshot
  src="https://res.cloudinary.com/demo/image/upload/v1/netlify/deploy-step4-auto-deploy-ko.png"
  alt="자동 배포 진행 중"
  caption="GitHub에 푸시할 때마다 자동 배포가 트리거됩니다"
/>

</Step>

---

<Step number={5} title="커스텀 도메인 및 프로덕션 설정 구성">

1. Netlify 사이트 대시보드에서 **도메인 설정**을 클릭합니다.
2. (선택사항) **커스텀 도메인 추가**를 클릭하여 자신의 도메인을 사용합니다 (mysite.com).
3. 도메인 제공자로 DNS 레코드를 업데이트합니다.
4. Netlify는 **무료 HTTPS** - 자동으로 활성화됩니다.
5. 사이트는 이제 프로덕션 준비가 되었습니다!

<Screenshot
  src="https://res.cloudinary.com/demo/image/upload/v1/netlify/deploy-step5-domain-ko.png"
  alt="도메인 구성"
  caption="커스텀 도메인 추가 및 HTTPS 활성화"
/>

<Checkpoint
  title="연속 배포 확인"
  items={[
    "사이트가 성공적으로 배포되었나요?",
    "라이브 URL에 접근할 수 있나요?",
    "푸시에서 자동 배포가 트리거되었나요?",
    "HTTPS와 SSL이 활성화되어 있나요?"
  ]}
/>

<Callout type="success">
✅ **완료!** 사이트는 이제 GitHub에서 자동 배포와 함께 Netlify에 배포됩니다. 모든 푸시가 새 배포를 트리거합니다 - 수동 단계가 필요 없습니다!
</Callout>

</Step>

---

## 다음 단계

1. **커스텀 도메인 추가** - 도메인을 Netlify로 포인트합니다
2. **환경 변수 설정** - API 키와 비밀을 안전하게 저장합니다
3. **브랜치 배포 활성화** - 병합하기 전에 풀 리퀘스트 미리보기
4. **리다이렉트 구성** - URL 리다이렉션 및 재쓰기 설정

---

## 자주 묻는 질문

<Callout type="info">
**Q. 배포는 얼마나 자주 트리거되나요?**

A. 메인 브랜치에 푸시할 때마다 트리거됩니다. 각 푸시는 몇 초 내에 새 빌드와 배포를 트리거합니다.
</Callout>

<Callout type="info">
**Q. 배포하기 전에 변경사항을 테스트할 수 있나요?**

A. 네! Netlify는 풀 리퀘스트를 위해 **미리보기 배포**를 생성합니다. 메인으로 병합하기 전에 변경사항을 테스트할 수 있습니다.
</Callout>

<Callout type="info">
**Q. 배포가 실패하면 어떻게 하나요?**

A. **즉시 롤백**할 수 있습니다. 배포 히스토리의 이전 빌드에서 "배포 트리거"를 클릭합니다.
</Callout>

<Callout type="info">
**Q. 다른 브랜치에서 배포할 수 있나요?**

A. 네! 서로 다른 브랜치를 다른 URL에 배포하도록 구성합니다. 스테이징 환경에 좋습니다.
</Callout>

---

## 문제 해결

### 빌드 실패 - npm 의존성 필요

- `package.json`이 저장소 루트에 있는지 확인합니다
- 모든 의존성이 나열되어 있는지 확인합니다 (npm install이 로컬에서 작동함)
- 빌드 로그에서 특정 오류 메시지를 확인합니다

### 사이트는 배포 후 404 오류를 표시합니다

- 게시 디렉토리가 올바른지 확인합니다
- 빌드 출력 디렉토리가 존재하는지 확인합니다
- React/Next.js의 경우 빌드 명령이 성공적으로 실행되었는지 확인합니다

### 자동 배포가 트리거되지 않았습니다

- GitHub 통합이 여전히 인증되었는지 확인합니다
- 올바른 브랜치에 푸시했는지 확인합니다 (main/master)
- "배포" 탭에서 푸시 이벤트 히스토리를 봅니다
- 수동 배포 트리거: "배포 트리거" 클릭

### HTTPS가 작동하지 않습니다

- SSL 인증서 생성이 시간이 걸리므로 몇 분을 기다립니다
- 도메인 설정으로 이동하여 HTTPS 상태를 확인합니다
- 수동 도메인인 경우 DNS가 Netlify로 포인트하는지 확인합니다

---

**축하합니다!** 웹사이트는 이제 연속 통합으로 배포됩니다. 모든 푸시가 자동으로 사이트를 업데이트합니다! 🚀
